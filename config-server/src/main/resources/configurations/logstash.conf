input {
  tcp {
    port => 5044
    codec => json_lines
  }
}

output {
  elasticsearch {
    hosts => ["http://localhost:9200"]
    index => "microservices-%{+YYYY.MM.dd}"
  }
  # Useful for debugging in Podman logs
  stdout { codec => rubydebug }
}